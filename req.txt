when app is opened a landing page is shown with a link to a notes page

when a link is clicked a page with existing notes is listed on the left comumn with empty fields to enter a new note title and the new note's text on the right column

when a new note title and it's text are written a SAVE icon appers in the navigation at top of the page

when the SAVE icon is clicked the new note entered is saved and appears in the left column with the other existing notes

when an existing note is clicked in the list on the left the note appears on the right

when the write icon is clicked in the navigation on the top of the page then empty fields are shown to eveter a new note title and it's text on the right column


Application should include:
db.json - used to store and retrieve notes using the (fs) module
GET /notes - returns the notes.html file
GET * - returns the index.html file
GET /api/notes should read the db.json file and return all saved notes as JSON
POST /api/notes should revieve a new note to save on the request body, add it ot the db.json file and return to client


